type: object

description: Envelope for the paginated message list.

required:
  - data
  - meta

properties:

  data:
    type: array
    items:
      $ref: "./MessageSummary.yaml"

  meta:
    type: object

    required:
      - page
      - limit
      - total
      - pages

    properties:

      page:
        type: integer
        minimum: 1

      limit:
        type: integer
        minimum: 1
        maximum: 100

      total:
        type: integer
        description: Total matching messages.

      pages:
        type: integer