type: object

description: Dry-run segment resolution result. No automations are created and no PII is returned.

required:
  - data

properties:

  data:
    type: object

    required:
      - count

    properties:

      count:
        type: integer
        description: Qualifying customers (deduplicated, most recent purchase first).