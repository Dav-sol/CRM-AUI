type: object

description: Envelope for the paginated customer list.

required:
  - data
  - meta

properties:

  data:
    type: array
    items:
      $ref: "./CustomerSummary.yaml"

  meta:
    type: object

    required:
      - page
      - limit
      - total
      - pages

    properties:

      page:
        type: integer
        minimum: 1

      limit:
        type: integer
        minimum: 1
        maximum: 100

      total:
        type: integer
        description: Total matching customers (soft-deleted excluded).

      pages:
        type: integer