type: object

description: Envelope for the paginated commercial cycle list.

required:
  - data
  - meta

properties:

  data:
    type: array
    items:
      $ref: "./CommercialCycleSummary.yaml"

  meta:
    type: object

    required:
      - page
      - limit
      - total
      - pages

    properties:

      page:
        type: integer
        minimum: 1

      limit:
        type: integer
        minimum: 1
        maximum: 100

      total:
        type: integer
        description: Total matching commercial cycles.

      pages:
        type: integer